package models.sensors.data;

import models.sensors.Sensor;

class RawSensorData extends SensorData {

    override private function update(newValues:Array<Float>)
    {
        super.update(newValues);
    }

}